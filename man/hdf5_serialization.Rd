% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdf5_serialization.R
\name{Read and write routes and intervals}
\alias{Read and write routes and intervals}
\alias{write_r_object_h5}
\alias{read_r_object_h5}
\alias{read_Routes}
\alias{write_Routes}
\alias{read_BirdFlowRoutes}
\alias{write_BirdFlowRoutes}
\alias{read_intervals}
\alias{write_intervals}
\alias{write_routes}
\alias{write_routes.Routes}
\alias{write_routes.BirdFlowRoutes}
\alias{write_routes.default}
\alias{read_routes}
\title{Read and write routes and intervals}
\usage{
write_routes(obj, path)

\method{write_routes}{Routes}(obj, path)

\method{write_routes}{BirdFlowRoutes}(obj, path)

\method{write_routes}{default}(obj, path)

read_routes(path)

read_intervals(path)

write_intervals(obj, path)
}
\arguments{
\item{obj}{For \code{write_routes()} a \code{Routes}, or \code{BirdFlowRoutes} object.
For \code{write_intevals()} a \code{BirdFlowIntervals} object.}

\item{path}{Path to write or read from, should end in \code{.hdf5}}
}
\value{
\itemize{
\item \code{write_routes()} ans \code{write_invervals()} invisibly return their
input object (after writing to disk).
\item \code{read_routes()} returns the written object - either \code{Routes} or
\code{BirdFlowRoutes}.
\item \code{read_intervals()} return the written \code{BirdFlowIntervals} object.
}
}
\description{
These functions provide support reading and writing routes and intervals
from and to HDF5 files while preserving class names and attributes.
}
\details{
The Hierarchical Data Format version 5 (HDF5) is an open format to
efficiently store large, heterogeneous data sets. HDF5 files can
be written and read from R and Python among other languages so make a
good cross-language format for routes and intervals.

\code{read_routes()} and \code{write_routes()} work on both \code{Routes} and
\code{BirdFlowRoutes}.  See \code{\link[=Routes]{Routes()}} and \code{\link[=as_BirdFlowRoutes]{as_BirdFlowRoutes()}} to create
objects of these classes.

\code{read_intervals()} and \code{write_intervals()} work on \code{BirdFlowIntervals}
which are a collection of movements each with a single start and end
derived from \code{BirdFlowRoutes()}. See \code{\link[=as_BirdFlowIntervals]{as_BirdFlowIntervals()}}.
}
\section{Internal functions}{

\itemize{
\item \code{write_r_object_h5()} and \code{read_r_object_h5()} are internal workhorses:
they handle the recursive traversal of R lists (including data.frames,
Date/POSIX/Factor vectors) and store both data and class attributes
in the HDF5 file hierarchy.
}
}

\seealso{
\itemize{
\item \link[rhdf5:h5_createFile]{rhdf5::h5createFile}, \link[rhdf5:h5_write]{rhdf5::h5write}, \link[rhdf5:h5_read]{rhdf5::h5read}
for low-level HDF5 operations.
\item \code{\link[=import_birdflow]{import_birdflow()}}, and \code{\link[=export_birdflow]{export_birdflow()}} for reading and writing
BirdFlow models. Note \code{\link[=import_birdflow]{import_birdflow()}} also substantially
alters the object if it has not previously been imported into R.
}
}
\keyword{internal}
