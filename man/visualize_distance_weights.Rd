% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_distance_weights.R
\name{visualize_distance_weights}
\alias{visualize_distance_weights}
\title{Visualize the spread kernel used by \code{\link[=calc_dist_weights]{calc_dist_weights()}}}
\usage{
visualize_distance_weights(
  line_lengths = c(2000, 1000, 500, 200),
  radius_m = NULL,
  res_m = 1000,
  kernel = c("m3", "bb", "m1", "m5", "sq"),
  gamma = 3e+10,
  kl = 9e+05,
  s1 = 200,
  type = c("envelope", "raster"),
  n = 100
)
}
\arguments{
\item{line_lengths}{A vector of transition-line lengths (m) to visualize
the spread kernel for.}

\item{radius_m}{The detection radius (m). Defaults to \code{res_m / 2} (the
same default used by \code{\link[=calc_bmtr]{calc_bmtr()}}'s continuous methods). For
\code{type = "envelope"} this is only shown as a reference line, and only if
explicitly supplied. For \code{type = "raster"} it's used, as in
\code{\link[=calc_dist_weights]{calc_dist_weights()}}, to determine the band of probability density
that forms the plotted weight.}

\item{res_m}{The resolution of the associated bird flow model (m), used
to determine the nugget added to the variance. See \code{\link[=calc_dist_weights]{calc_dist_weights()}}.}

\item{kernel}{The kernel used for calculating the standard deviation
in the probability distribution:
\describe{
\item{\code{"m3"}}{(default) Matern 3/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m1"}}{Matern 1/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m5"}}{Matern 5/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"sq"}}{Squared-exponential (Gaussian). Driven by \code{gamma} and \code{kl}.}
\item{\code{"bb"}}{Brownian bridge. Driven by \code{s1}.}
}
See \code{\link[=visualize_distance_weights]{visualize_distance_weights()}} to explore how these kernels and their
hyperparameters shape the spread.}

\item{gamma}{Spread magnitude hyperparameter (m^2, a variance) for the
Matern-family and squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"},
\code{"sq"}). Ignored for \code{kernel = "bb"}. The default was tuned by eye against
\code{visualize_distance_weights()} for a ~100-150 km resolution model; see
\code{dev/kernel_parameter_tuning.Rmd}.}

\item{kl}{Lengthscale hyperparameter (m) for the Matern-family and
squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"}, \code{"sq"}). Ignored for
\code{kernel = "bb"}. The default was tuned by eye against
\code{visualize_distance_weights()} for a ~100-150 km resolution model; see
\code{dev/kernel_parameter_tuning.Rmd}.}

\item{s1}{Spread magnitude hyperparameter (units of sqrt(m), not m) for
the \code{"bb"} kernel. Ignored for all other kernels. The default was tuned by
eye against \code{visualize_distance_weights()} for a ~100-150 km resolution
model; see \code{dev/kernel_parameter_tuning.Rmd}.}

\item{type}{The style of plot:
\describe{
\item{\code{"envelope"}}{(default) For each line length, plots the region
within 1.96 standard deviations of the line (in the kernel's probability
distribution) as a band along the line. This represents 95 percent of
the density.}
\item{\code{"raster"}}{For each line length, plots the actual weight returned
by \code{\link[=calc_dist_weights]{calc_dist_weights()}} over a grid of points around the line, as a
heatmap. Slower, but reflects \code{radius_m}'s effect on the weight as well
as the kernel's spread.}
}}

\item{n}{The number of grid points used along (and, for \code{type = "raster"},
across) each line.}
}
\value{
A \code{ggplot2} object. It can be displayed with \code{print()}.
}
\description{
\code{visualize_distance_weights()} plots the shape of the spread kernel used
by \code{\link[=calc_dist_weights]{calc_dist_weights()}} (and, through it, the \code{"continuous"} and
\code{"continuous-spherical"} methods of \code{\link[=calc_bmtr]{calc_bmtr()}}) for one or more
transition-line lengths. It's meant as a tool for choosing \code{kernel} and
its hyperparameters (\code{gamma}, \code{kl}, \code{s1}) before running an expensive
\code{calc_bmtr()} call.
}
\examples{
visualize_distance_weights()
visualize_distance_weights(type = "raster", kernel = "bb", s1 = 20)
}
\seealso{
\code{\link[=calc_dist_weights]{calc_dist_weights()}}, \code{\link[=calc_bmtr]{calc_bmtr()}}
}
