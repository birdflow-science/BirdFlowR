% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_euclidean_detection_rate.R
\name{project_points_simple}
\alias{project_points_simple}
\title{Project points onto line segments (Euclidean/planar geometry)}
\usage{
project_points_simple(s, e, p, m, n, v, vv, clamp = FALSE, tol = 0)
}
\arguments{
\item{s}{An n x 2 matrix of segment start coordinates.}

\item{e}{An n x 2 matrix of segment end coordinates.}

\item{p}{An m x 2 matrix of point coordinates.}

\item{m}{Number of points (\code{nrow(p)}).}

\item{n}{Number of line segments (\code{nrow(s)}).}

\item{v}{\code{e - s}, precomputed segment vectors.}

\item{vv}{\code{rowSums(v^2)}, precomputed squared segment lengths.}

\item{clamp}{If \code{TRUE} constrain the projected fraction to \verb{[0, 1]}. If
\code{FALSE} (default) projections falling outside the segment are set to \code{NA}.}

\item{tol}{Segments with squared length at or below this threshold are
treated as having zero length (projected fraction set to 0).}
}
\value{
A list with \code{dist_to_line} and \code{dist_along}, each a vector of
length \code{m * n} (point index varying fastest).
}
\description{
Vectorized computation of the distance from each point in \code{P} to each
line segment defined by \code{S} (starts) and \code{E} (ends), and the distance
along the segment to the projection of the point.
}
\keyword{internal}
