% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_distance_weights.R
\name{calc_dist_weights}
\alias{calc_dist_weights}
\title{calculate the weights of transitions for bmtr points}
\usage{
calc_dist_weights(
  dist_to_line,
  dist_along_line,
  line_lengths,
  radius_m,
  res_m,
  kernel = c("m3", "bb", "m1", "m5", "sq"),
  gamma = 3e+10,
  kl = 9e+05,
  s1 = 200
)
}
\arguments{
\item{dist_to_line}{How far is the point from the line (m)}

\item{dist_along_line}{How far along the line is the point, after
projecting it  onto the line (m)}

\item{line_lengths}{How long is the line (m)}

\item{radius_m}{The radius of the transect at the bmtr points - used to
determine the band of probability density that will be added to form
the weight.}

\item{res_m}{The resolution of the associated bird flow model, used to
determine the nugget added to the variance to represent the uncertainty in
the starting and ending location of the transition.}

\item{kernel}{The kernel used for calculating the standard deviation
in the probability distribution:
\describe{
\item{\code{"m3"}}{(default) Matern 3/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m1"}}{Matern 1/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m5"}}{Matern 5/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"sq"}}{Squared-exponential (Gaussian). Driven by \code{gamma} and \code{kl}.}
\item{\code{"bb"}}{Brownian bridge. Driven by \code{s1}.}
}
See \code{\link[=visualize_distance_weights]{visualize_distance_weights()}} to explore how these kernels and their
hyperparameters shape the spread.}

\item{gamma}{Spread magnitude hyperparameter (m^2, a variance) for the
Matern-family and squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"},
\code{"sq"}). Ignored for \code{kernel = "bb"}. The default was tuned by eye with
\code{visualize_distance_weights()}.}

\item{kl}{Lengthscale hyperparameter (m) for the Matern-family and
squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"}, \code{"sq"}). Ignored for
\code{kernel = "bb"}. The default was tuned by eye using
\code{visualize_distance_weights()}.}

\item{s1}{Spread magnitude hyperparameter (units of sqrt(m), not m) for
the \code{"bb"} kernel. Ignored for all other kernels. The default was tuned by
eye against \code{visualize_distance_weights()}.}
}
\value{
A vector of weights of the same length as the first three arguments.
}
\description{
\code{calc_dist_weights()} is an internal function that takes summary stats
on the relationship between points and a transition line and returns
the weight that should be used for that transition.
}
\details{
The first three arguments can all be vectors in which case the calculations
will be vectorized over the corresponding elements.

This is a preliminary version of the function and will likely change.
}
\seealso{
\code{\link[=visualize_distance_weights]{visualize_distance_weights()}}
}
\keyword{internal}
