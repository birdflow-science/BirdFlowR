% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_clipped.R
\name{is_clipped}
\alias{is_clipped}
\title{Test whether a BirdFlow model was clipped during preprocessing}
\usage{
is_clipped(x)
}
\arguments{
\item{x}{A \code{BirdFlow} model.}
}
\value{
A logical scalar: \code{TRUE} if a clip was applied, \code{FALSE} if the
model was preprocessed without clipping, or \code{NA} if the metadata is
missing (legacy models).
}
\description{
\code{is_clipped()} reports whether a clip polygon was supplied to
\code{\link[=preprocess_species]{preprocess_species()}} when the model was built.
}
\details{
Returns \code{NA} for models built before clip metadata was recorded — there
is no way to recover this information from older HDF5 / RDS files. Use
\code{\link[=get_clip]{get_clip()}} to retrieve the polygon itself.
}
\examples{
  library(BirdFlowModels)
  is_clipped(amewoo) # NA — fixture predates clip metadata
}
\seealso{
\code{\link[=get_clip]{get_clip()}}, \code{\link[=preprocess_species]{preprocess_species()}}.
}
