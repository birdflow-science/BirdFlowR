% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ebird_coverage.R
\name{get_ebird_coverage}
\alias{get_ebird_coverage}
\title{Get eBird model coverage from a BirdFlow model}
\usage{
get_ebird_coverage(bf, format = "SpatRaster")
}
\arguments{
\item{bf}{A BirdFlow model.}

\item{format}{One of \code{"SpatRaster"} (default), \code{"array"}, or
\code{"dataframe"}. See the return section for details. Format strings are
matched case-insensitively; common aliases are accepted
(e.g. \code{"terra"}, \code{"numeric"}, \code{"matrix"}, \code{"data.frame"}).}
}
\value{
\code{NA} (with a warning) if the model lacks coverage metadata.
Otherwise the return type depends on \code{format}:
\itemize{
\item \code{"SpatRaster"} returns a multi-layer \link[terra:SpatRaster-class]{terra::SpatRaster} with one
logical layer per timestep. Layer names match the timestep labels
(e.g. \code{"t1"}, \code{"t2"}, ...).
\item \code{"array"} returns the raw 3-D logical array
\verb{[row, col, time]} as stored in \code{metadata$ebird_model_coverage}.
\item \code{"dataframe"} returns a long \link{data.frame} suitable for plotting with
\code{\link[ggplot2:geom_tile]{ggplot2::geom_raster()}}, with one row per cell × timestep and
columns:
\itemize{
\item \code{row}, \code{col} — raster row and column indices.
\item \code{x}, \code{y} — cell-center coordinates in the model CRS.
\item \code{i} — location index in \code{bf}; \code{NA} for cells outside the mask.
\item \code{timestep} — timestep label (e.g. \code{"t1"}).
\item \code{coverage} — logical coverage value.
}
}
}
\description{
\code{get_ebird_coverage()} retrieves the per-timestep eBird model coverage
stored in a BirdFlow model. Each cell–timestep combination records whether
that cell fell within eBird's modeled extent at that timestep; eBird 2023
onward uses \code{NA} for cells with insufficient data (distinct from cells with
zero abundance).
}
\details{
Coverage metadata is captured by \code{\link[=preprocess_species]{preprocess_species()}} and is absent from
models preprocessed before \verb{BirdFlowR 0.1.0.9081}. For such models
\code{get_ebird_coverage()} returns \code{NA} with a warning.
}
\examples{
\dontrun{
bf <- preprocess_species("amewoo")
cov <- get_ebird_coverage(bf)               # SpatRaster, one layer per week
arr <- get_ebird_coverage(bf, "array")      # raw 3-D logical array
df  <- get_ebird_coverage(bf, "dataframe")  # long data frame for ggplot2
}
}
\seealso{
\code{\link[=preprocess_species]{preprocess_species()}} which captures this metadata,
\code{\link[=get_mask]{get_mask()}} for the static model mask.
}
