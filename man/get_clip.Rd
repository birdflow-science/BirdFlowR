% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_clip.R
\name{get_clip}
\alias{get_clip}
\title{Retrieve the clip polygon used during preprocessing}
\usage{
get_clip(x)
}
\arguments{
\item{x}{A \code{BirdFlow} model.}
}
\value{
An \code{sf} object containing the clip polygon, or \code{NULL} if the
model is unclipped or the clip metadata is unknown (older models
preprocessed before this metadata was captured).
}
\description{
Reconstructs the clip polygon that was supplied to \code{\link[=preprocess_species]{preprocess_species()}}
from the metadata stored on the \code{BirdFlow} object. Returns an
\link[sf:sf]{sf} polygon in the model's CRS (\code{crs(x)}).
}
\details{
The polygon is stored as a flat data frame (see \code{\link[=clip_to_dataframe]{clip_to_dataframe()}}) so
it can be serialized as part of the model. \code{get_clip()} delegates the
data-frame-to-polygon conversion to \code{\link[=dataframe_to_clip]{dataframe_to_clip()}} and wraps the
result in an \code{sf} object; pass the result through \code{terra::vect()} if you'd
prefer a \link[terra:SpatVector-class]{terra::SpatVector}.
}
\examples{
  library(BirdFlowModels)
  get_clip(amewoo) # NULL — fixture predates clip metadata
}
\seealso{
\code{\link[=is_clipped]{is_clipped()}}, \code{\link[=preprocess_species]{preprocess_species()}}, \code{\link[=dataframe_to_clip]{dataframe_to_clip()}}.
}
