% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_aligned.R
\name{project_aligned}
\alias{project_aligned}
\title{Project a SpatRaster onto a grid aligned to multiples of a target resolution}
\usage{
project_aligned(x, crs, res_m, method)
}
\arguments{
\item{x}{A SpatRaster.}

\item{crs}{Target coordinate reference system as accepted by
\code{\link[terra:crs]{terra::crs()}}.}

\item{res_m}{Target cell size in the units of \code{crs} (typically meters).}

\item{method}{Resampling method passed to \code{\link[terra:project]{terra::project()}}.}
}
\value{
A SpatRaster in \code{crs} with \code{res(...) == c(res_m, res_m)} and
cells aligned to multiples of \code{res_m}.
}
\description{
\code{project_aligned()} projects \code{x} into the target \code{crs} at resolution
\code{res_m}, with cells aligned to multiples of \code{res_m} (i.e. origin at 0,
0). It is a workaround for terra >= 1.9-25, which errors with
\verb{[write] unknown option(s): xscale,yscale} whenever \code{res} or \code{origin}
is passed alongside a CRS-string target in \code{\link[terra:project]{terra::project()}}. The
template-raster path used here is unaffected.
}
\details{
Internally it projects the source extent (cheap), snaps outward to
multiples of \code{res_m} with one cell of buffer on each side (to absorb
the difference between corner-only extent projection and the true
bounding box of a non-rectangular reprojected raster), constructs an
empty template SpatRaster with that grid, then projects \code{x} against
the template.
}
\keyword{internal}
