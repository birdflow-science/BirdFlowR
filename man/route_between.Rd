% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route_between.R
\name{route_between}
\alias{route_between}
\title{Generate BirdFlow routes between known locations}
\usage{
route_between(
  bf,
  n,
  x_coord = NULL,
  y_coord = NULL,
  date = NULL,
  potentials = NULL,
  ...
)
}
\arguments{
\item{bf}{A BirdFlow object.}

\item{n}{Number of routes to sample.}

\item{x_coord, y_coord}{Parallel numeric vectors of observed x and y
coordinates (in the model's CRS). Must be provided together with \code{date}.
Converted internally to indicator (one-hot) potentials. Cannot be used
together with \code{potentials}.}

\item{date}{When using \code{x_coord}/\code{y_coord}: a parallel vector of dates
corresponding to each observation. When using \code{potentials}: a vector of
dates corresponding to the columns of \code{potentials} (alternative to column
names). Accepts any format recognized by \code{\link[=lookup_timestep]{lookup_timestep()}}: date strings
(\code{"2023-03-29"}), \code{Date} objects, numeric timestep indices, or
\code{"t1"}-style strings.}

\item{potentials}{An \code{n_active(bf)} x \code{n_obs} matrix of soft observation
potentials. Each column is a non-negative vector representing the
observation likelihood at a given timestep. Timestep association is via
column names or the \code{date} argument (exactly one required). Cannot be used
together with \code{x_coord}/\code{y_coord}.}

\item{...}{
  Arguments passed on to \code{\link[=lookup_timestep_sequence]{lookup_timestep_sequence}}
  \describe{
    \item{\code{season}}{a season name, season alias, or "all".  See
\code{\link[=lookup_season_timesteps]{lookup_season_timesteps()}} for options.}
    \item{\code{start}}{The starting point in time specified as a
timestep, character date, or date object.}
    \item{\code{end}}{The ending point in time as a date or timestep.}
    \item{\code{direction}}{Either "forward" or "backward" defaults to
\code{"forward"} if not processing dates.  If using date input \code{direction} is
optional and is only used to verify the direction implicit in the dates.}
    \item{\code{season_buffer}}{Only used with \code{season} input. \code{season_buffer} is
passed to \code{\link[=lookup_season_timesteps]{lookup_season_timesteps()}} and defaults to 1; it is the number
of timesteps to extend the season by at each end.}
    \item{\code{n_steps}}{Alternative to \code{end}  The end will be \code{n_steps} away from
\code{start} in \code{direction}; and the resulting sequence will have \code{n_step}
transitions and \code{n_steps + 1}  timesteps.}
  }}
}
\value{
A \link{BirdFlowRoutes} object. Same format as \code{\link[=route]{route()}}.
}
\description{
\code{route_between()} generates synthetic migration routes conditioned on
observed locations at specific times, using a Hidden Markov Model
forward-filtering-backward-sampling algorithm.
}
\examples{
bf <- BirdFlowModels::amewoo
xy <- latlon_to_xy(lat = c(30.5, 45.5), lon = c(-91.5, -68.5), bf)
rts <- route_between(bf, n = 5,
                     x_coord = xy$x, y_coord = xy$y,
                     date = c("2023-02-15", "2023-05-01"))
plot_routes(rts)
}
\seealso{
\code{\link[=route]{route()}}, \code{\link[=predict]{predict()}}
}
