% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_raster_data.R
\name{format_raster_data}
\alias{format_raster_data}
\title{Format raster data from a BirdFlow model for output}
\usage{
format_raster_data(data, bf, format, value_name)
}
\arguments{
\item{data}{A 2-D logical matrix (e.g. the model mask) or 3-D logical
array (e.g. \code{ebird_coverage}) aligned to the BirdFlow model grid.
For 3-D arrays the third dimension is time; its \code{dimnames$time} element
is used to name raster layers and the \code{timestep} column.}

\item{bf}{A BirdFlow model (supplies grid geometry).}

\item{format}{A character string naming the output format. Recognized
values (case-insensitive, with aliases):
\itemize{
\item \code{"SpatRaster"} / \code{"spatrast"} / \code{"terra"} — \link[terra:SpatRaster-class]{terra::SpatRaster}
\item \code{"numeric"} / \code{"array"} / \code{"matrix"} — raw data returned as-is
\item \code{"dataframe"} / \code{"data.frame"} / \code{"raster.data.frame"} — long
\link{data.frame}
}}

\item{value_name}{Column name for the data values in \code{"dataframe"} output
and layer name for single-layer \code{"SpatRaster"} output.}
}
\value{
Depends on \code{format}:
\itemize{
\item \code{"spatraster"} — a \link[terra:SpatRaster-class]{terra::SpatRaster}; one layer per time slice for
3-D input, a single named layer for 2-D input.
\item \code{"numeric"} — \code{data} returned as-is.
\item \code{"dataframe"} — a \link{data.frame} with columns \code{row}, \code{col}, \code{x}, \code{y},
\code{i} (NA outside the mask), and \verb{<value_name>}. For 3-D input the
frame is in long form (one row per cell × timestep) with an
additional \code{timestep} column preceding \verb{<value_name>}.
}
}
\description{
Internal helper shared by \code{\link[=get_mask]{get_mask()}} and \code{\link[=get_ebird_coverage]{get_ebird_coverage()}}.
Handles format-string normalization, alias resolution, and the three
output formats.
}
\seealso{
\code{\link[=get_mask]{get_mask()}}, \code{\link[=get_ebird_coverage]{get_ebird_coverage()}}
}
\keyword{internal}
