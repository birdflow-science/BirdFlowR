% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframe_to_clip.R
\name{dataframe_to_clip}
\alias{dataframe_to_clip}
\title{Reconstruct a clip polygon from its data frame representation}
\usage{
dataframe_to_clip(df, crs = NA)
}
\arguments{
\item{df}{A data frame as produced by \code{\link[=clip_to_dataframe]{clip_to_dataframe()}} with columns
\code{id}, \code{part}, \code{x}, \code{y}, and \code{hole}. The \code{hole} column is the integer
ring index (\code{0} for outer rings, \verb{1..N} for each distinct hole within
a part); legacy logical values are coerced via \code{as.integer()}.}

\item{crs}{Coordinate reference system. Accepts anything \code{\link[terra:vect]{terra::vect()}}
accepts (a WKT string, an \code{"EPSG:..."} code, an \code{\link[sf:st_crs]{sf::st_crs()}} object,
or \code{NA} for no CRS).}
}
\value{
An \link[sf:sfc]{sfc} polygon (geometry only, no attributes).
}
\description{
Inverse of \code{\link[=clip_to_dataframe]{clip_to_dataframe()}}. Takes the flat data frame stored in a
\code{BirdFlow} object's \code{metadata$clip$polygon} slot and rebuilds an
\link[sf:sfc]{sf} polygon. Factored out of \code{\link[=get_clip]{get_clip()}} so the round-trip
(polygon -> data frame -> polygon) can be exercised in tests without
constructing a full \code{BirdFlow} object.
}
\seealso{
\code{\link[=clip_to_dataframe]{clip_to_dataframe()}}, \code{\link[=get_clip]{get_clip()}}.
}
\keyword{internal}
