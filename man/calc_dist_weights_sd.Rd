% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_distance_weights.R
\name{calc_dist_weights_sd}
\alias{calc_dist_weights_sd}
\title{Standard deviation of the spread kernel used by \code{\link[=calc_dist_weights]{calc_dist_weights()}}}
\usage{
calc_dist_weights_sd(
  dist_along_line,
  line_lengths,
  res_m,
  kernel,
  gamma,
  kl,
  s1
)
}
\arguments{
\item{dist_along_line}{How far along the line the point of interest is (m)}

\item{line_lengths}{How long the line is (m)}

\item{res_m}{The resolution of the associated bird flow model, used to
determine the nugget added to the variance to represent the uncertainty in
the starting and ending location of the transition.}

\item{kernel}{The kernel used for calculating the standard deviation
in the probability distribution:
\describe{
\item{\code{"m3"}}{(default) Matern 3/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m1"}}{Matern 1/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"m5"}}{Matern 5/2. Driven by \code{gamma} and \code{kl}.}
\item{\code{"sq"}}{Squared-exponential (Gaussian). Driven by \code{gamma} and \code{kl}.}
\item{\code{"bb"}}{Brownian bridge. Driven by \code{s1}.}
}
See \code{\link[=visualize_distance_weights]{visualize_distance_weights()}} to explore how these kernels and their
hyperparameters shape the spread.}

\item{gamma}{Spread magnitude hyperparameter (m^2, a variance) for the
Matern-family and squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"},
\code{"sq"}). Ignored for \code{kernel = "bb"}. The default was tuned by eye against
\code{visualize_distance_weights()} for a ~100-150 km resolution model; see
\code{dev/kernel_parameter_tuning.Rmd}.}

\item{kl}{Lengthscale hyperparameter (m) for the Matern-family and
squared-exponential kernels (\code{"m1"}, \code{"m3"}, \code{"m5"}, \code{"sq"}). Ignored for
\code{kernel = "bb"}. The default was tuned by eye against
\code{visualize_distance_weights()} for a ~100-150 km resolution model; see
\code{dev/kernel_parameter_tuning.Rmd}.}

\item{s1}{Spread magnitude hyperparameter (units of sqrt(m), not m) for
the \code{"bb"} kernel. Ignored for all other kernels. The default was tuned by
eye against \code{visualize_distance_weights()} for a ~100-150 km resolution
model; see \code{dev/kernel_parameter_tuning.Rmd}.}
}
\value{
A vector of standard deviations (m), the same length as
\code{dist_along_line}.
}
\description{
Computes the standard deviation (in m) of the probability distribution
representing uncertainty in a bird's location at a given point along a
transition line, for one of the kernels supported by
\code{\link[=calc_dist_weights]{calc_dist_weights()}}. Factored out of \code{\link[=calc_dist_weights]{calc_dist_weights()}} so
\code{\link[=visualize_distance_weights]{visualize_distance_weights()}} can plot the same spread it uses without
duplicating the kernel-selection logic.
}
\keyword{internal}
