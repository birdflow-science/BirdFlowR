# Sort Routes by ID and Date

Sorts a `Routes` or data frame object by route ID and date.

## Usage

``` r
sort_by_id_and_dates(routes)
```

## Arguments

- routes:

  A `Routes` or data frame object.

## Value

A sorted data frame.
