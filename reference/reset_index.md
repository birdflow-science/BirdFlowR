# Reset Route Indices

Resets the route IDs in a `Routes` object to a new sequential numbering.

## Usage

``` r
reset_index(routes)
```

## Arguments

- routes:

  A `Routes` or data frame object.

## Value

A data frame with updated route IDs.
